spring:
  application:
    name: "Book Recommendation System"
  datasource:
    url: jdbc:postgresql://localhost:5432/recommendation_book_system_db
    username: postgres
    password: 123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        default_schema: book_recommendation_system
        format_sql: true
    show-sql: true
    open-in-view: false

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.type.descriptor.sql.BasicBinder: TRACE
    org.hibernate.engine.jdbc.spi.SqlExceptionHelper: WARN
    com.zaxxer.hikari: INFO
    org.hibernate.orm.connections.pooling: WARN
    org.springframework.jdbc.datasource.DataSourceUtils: INFO

api:
  prefix: /api/v1
cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:http://localhost:5173,http://localhost:5174}
auth:
  token:
    jwtSecret: ${JWT_SECRET:fwaqefhwaeufhasufhaowufhaweiofhwoe1234124awefawwerwaer21324}
    accessExpirationInMils: 7200000